# 🌳 ÁRVORE DE DEPENDÊNCIAS: Funções de Log

**Data:** 16/11/2025  
**Objetivo:** Visualizar a hierarquia de chamadas entre funções de log  
**Status:** ✅ **ANÁLISE CONCLUÍDA**

---

## 🎯 OBJETIVO

Criar uma árvore visual mostrando quais funções de log chamam outras funções de log, identificando:
- Hierarquia de chamadas
- Dependências circulares (loops infinitos)
- Caminhos de execução

---

## 📊 ÁRVORE DE DEPENDÊNCIAS

### **JAVASCRIPT**

```
┌─────────────────────────────────────────────────────────────────┐
│                    CÓDIGO DO PROJETO                            │
│  (FooterCodeSiteDefinitivoCompleto.js,                          │
│   webflow_injection_limpo.js, MODAL_WHATSAPP_DEFINITIVO.js)     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ (chama)
                              ▼
        ┌─────────────────────────────────────────┐
        │  1. logClassified()                     │
        │  (519+ ocorrências)                      │
        │  ❌ NÃO persiste no banco                │
        └─────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
                │ (chama)                   │ (NÃO chama atualmente)
                ▼                           ▼
        ┌──────────────────┐      ┌──────────────────────────────┐
        │ console.log()    │      │ sendLogToProfessionalSystem()│
        │ console.error()  │      │ ⚠️ DEVERIA ser chamado aqui  │
        │ console.warn()   │      │ ❌ NÃO é chamado             │
        └──────────────────┘      └──────────────────────────────┘
                                            │
                                            │ (chama)
                                            ▼
                            ┌──────────────────────────────┐
                            │ log_endpoint.php (HTTP POST) │
                            └──────────────────────────────┘
                                            │
                                            │ (chama)
                                            ▼
                            ┌──────────────────────────────┐
                            │ ProfessionalLogger (PHP)      │
                            │ ✅ Persiste no banco           │
                            └──────────────────────────────┘

        ┌─────────────────────────────────────────┐
        │  2. sendLogToProfessionalSystem()        │
        │  ✅ Persiste no banco                    │
        └─────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
                │ (chama)                   │ (chama)
                ▼                           ▼
        ┌──────────────────┐      ┌──────────────────────────────┐
        │ logClassified()   │      │ log_endpoint.php (HTTP POST) │
        │ ⚠️ LOOP INFINITO! │      │ ✅ Persiste no banco          │
        │ (linhas 430, 435, │      └──────────────────────────────┘
        │  441, 442, 455,  │
        │  510-524, 538-600)│
        └──────────────────┘

        ┌─────────────────────────────────────────┐
        │  3. logUnified() (DEPRECATED)            │
        │  ✅ Persiste no banco                    │
        └─────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
                │ (chama)                   │ (chama)
                ▼                           ▼
        ┌──────────────────┐      ┌──────────────────────────────┐
        │ console.log()    │      │ sendLogToProfessionalSystem() │
        │ console.error()  │      │ ✅ Persiste no banco          │
        │ console.warn()   │      └──────────────────────────────┘
        └──────────────────┘

        ┌─────────────────────────────────────────┐
        │  4. logInfo() / logError() / logWarn()   │
        │  logDebug() (ALIASES - DEPRECATED)        │
        └─────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
                │ (chama)                   │ (fallback)
                ▼                           ▼
        ┌──────────────────┐      ┌──────────────────────────────┐
        │ logClassified()  │      │ logUnified() (DEPRECATED)    │
        │ (preferencial)   │      │ (se logClassified não existe) │
        └──────────────────┘      └──────────────────────────────┘

        ┌─────────────────────────────────────────┐
        │  5. debugLog()                            │
        │  (MODAL_WHATSAPP_DEFINITIVO.js)          │
        └─────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
                │ (chama)                   │ (fallback)
                ▼                           ▼
        ┌──────────────────┐      ┌──────────────────────────────┐
        │ logClassified()  │      │ console.log/error/warn/debug  │
        │ (preferencial)   │      │ (se logClassified não existe) │
        └──────────────────┘      └──────────────────────────────┘
                │
                │ (também chama)
                ▼
        ┌──────────────────────────────┐
        │ logDebug() (window.logDebug)  │
        │ ⚠️ Pode causar loop           │
        └──────────────────────────────┘

        ┌─────────────────────────────────────────┐
        │  6. logEvent()                           │
        │  (MODAL_WHATSAPP_DEFINITIVO.js)         │
        └─────────────────────────────────────────┘
                              │
                              │ (chama)
                              ▼
        ┌─────────────────────────────────────────┐
        │ logClassified()                          │
        │ ❌ NÃO persiste no banco                 │
        └─────────────────────────────────────────┘

        ┌─────────────────────────────────────────┐
        │  7. console.log/error/warn (DIRETO)     │
        │  ❌ Não padronizado                      │
        └─────────────────────────────────────────┘
                              │
                              │ (não chama outras funções)
                              ▼
                        [FIM DA CADEIA]
```

---

### **PHP**

```
┌─────────────────────────────────────────────────────────────────┐
│                    CÓDIGO DO PROJETO                            │
│  (add_flyingdonkeys.php, add_webflow_octa.php, etc.)            │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ (chama)
                              ▼
        ┌─────────────────────────────────────────┐
        │  1. logDevWebhook() / logProdWebhook()   │
        │  (130+ ocorrências)                      │
        │  ❌ NÃO persiste no banco                │
        └─────────────────────────────────────────┘
                              │
                              │ (chama)
                              ▼
        ┌─────────────────────────────────────────┐
        │ file_put_contents() (arquivo texto)      │
        │ ❌ NÃO persiste no banco                 │
        └─────────────────────────────────────────┘
                              │
                              │ (também chama)
                              ▼
        ┌─────────────────────────────────────────┐
        │ error_log() (stderr/logs do PHP)         │
        │ ❌ NÃO persiste no banco                 │
        └─────────────────────────────────────────┘

        ┌─────────────────────────────────────────┐
        │  2. ProfessionalLogger                   │
        │  ✅ Persiste no banco                     │
        │  Métodos: info(), error(), warn(),       │
        │          debug(), fatal()                │
        └─────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
                │ (chama)                   │ (chama em caso de erro)
                ▼                           ▼
        ┌──────────────────┐      ┌──────────────────────────────┐
        │ insertLog()      │      │ logToFile() (fallback)       │
        │ (banco de dados) │      │ (arquivo texto)              │
        └──────────────────┘      └──────────────────────────────┘

        ┌─────────────────────────────────────────┐
        │  3. logDebug() (log_endpoint.php)        │
        │  ❌ NÃO persiste no banco                │
        └─────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                │                           │
                │ (chama)                   │ (chama)
                ▼                           ▼
        ┌──────────────────┐      ┌──────────────────────────────┐
        │ file_put_contents()│     │ error_log()                  │
        │ (arquivo texto)   │      │ (stderr/logs do PHP)          │
        └──────────────────┘      └──────────────────────────────┘

        ┌─────────────────────────────────────────┐
        │  4. error_log() (DIRETO)                 │
        │  ❌ Não padronizado                       │
        └─────────────────────────────────────────┘
                              │
                              │ (não chama outras funções)
                              ▼
                        [FIM DA CADEIA]
```

---

## ⚠️ DEPENDÊNCIAS CIRCULARES (LOOPS INFINITOS)

### **🔴 LOOP INFINITO CRÍTICO IDENTIFICADO:**

```
sendLogToProfessionalSystem()
         │
         │ (chama logClassified nas linhas 430, 435, 441, 442, 455, 510-524, 538-600)
         ▼
logClassified()
         │
         │ (DEVERIA chamar sendLogToProfessionalSystem, mas NÃO chama atualmente)
         │ (SE chamar, causaria loop infinito)
         ▼
[FIM - mas se adicionar chamada, causa loop]
```

**Status Atual:**
- ✅ `logClassified()` **NÃO** chama `sendLogToProfessionalSystem()` → **SEM loop atualmente**
- ⚠️ `sendLogToProfessionalSystem()` **chama** `logClassified()` → **Pode causar loop se `logClassified()` for modificado**

**Solução Necessária:**
- Substituir TODAS as chamadas `logClassified()` dentro de `sendLogToProfessionalSystem()` por `console.log/warn/error` direto
- OU adicionar flag de prevenção de recursão

---

## 📊 RESUMO DA HIERARQUIA

### **JavaScript - Cadeias de Chamada:**

1. **Cadeia Principal (NÃO persiste no banco):**
   ```
   Código → logClassified() → console.log/error/warn → [FIM]
   ```

2. **Cadeia Deprecated (persiste no banco):**
   ```
   Código → logUnified() → sendLogToProfessionalSystem() → log_endpoint.php → ProfessionalLogger → [BANCO]
   ```

3. **Cadeia de Aliases:**
   ```
   Código → logInfo/Error/Warn/Debug() → logClassified() → console.log → [FIM]
   ```

4. **Cadeia do Modal:**
   ```
   Código → debugLog() → logClassified() → console.log → [FIM]
   Código → logEvent() → logClassified() → console.log → [FIM]
   ```

5. **Cadeia Direta (persiste no banco):**
   ```
   Código → sendLogToProfessionalSystem() → log_endpoint.php → ProfessionalLogger → [BANCO]
   ```

### **PHP - Cadeias de Chamada:**

1. **Cadeia de Webhooks (NÃO persiste no banco):**
   ```
   Código → logDevWebhook() / logProdWebhook() → file_put_contents() → [ARQUIVO TEXTO]
   Código → logDevWebhook() / logProdWebhook() → error_log() → [STDERR]
   ```

2. **Cadeia Profissional (persiste no banco):**
   ```
   Código → ProfessionalLogger → insertLog() → [BANCO]
   Código → ProfessionalLogger → logToFile() (fallback) → [ARQUIVO TEXTO]
   ```

3. **Cadeia de Debug:**
   ```
   Código → logDebug() → file_put_contents() → [ARQUIVO TEXTO]
   Código → logDebug() → error_log() → [STDERR]
   ```

---

## 🎯 PONTOS CRÍTICOS

### **1. Loop Infinito Potencial:**
- ⚠️ `sendLogToProfessionalSystem()` chama `logClassified()`
- ⚠️ Se `logClassified()` chamar `sendLogToProfessionalSystem()`, causa loop infinito
- ✅ **Status atual:** Loop não ocorre porque `logClassified()` não chama `sendLogToProfessionalSystem()`

### **2. Logs Não Persistem:**
- ❌ `logClassified()` (519+ ocorrências) não persiste no banco
- ❌ `logDevWebhook()` / `logProdWebhook()` (153+ ocorrências) não persistem no banco
- ❌ `debugLog()` / `logEvent()` não persistem no banco

### **3. Múltiplas Cadeias:**
- ⚠️ 7 tipos diferentes de funções JavaScript
- ⚠️ 6 tipos diferentes de funções PHP
- ⚠️ Falta de padronização

---

## ✅ CONCLUSÃO

**Árvore de dependências criada com sucesso!**

**Principais descobertas:**
1. ✅ Identificado loop infinito potencial entre `sendLogToProfessionalSystem()` e `logClassified()`
2. ✅ Mapeadas todas as cadeias de chamada
3. ✅ Identificados pontos críticos de não persistência no banco
4. ✅ Visualizada hierarquia completa de dependências

**Próximos passos:**
- Implementar projeto simplificado para corrigir dependências
- Eliminar loop infinito potencial
- Garantir que todas as funções persistam no banco

---

**Status:** ✅ **ÁRVORE CRIADA**  
**Última atualização:** 16/11/2025

